import { NgxSimplePrintDirective } from './ngx-simple-print.directive';

describe('NgxSimplePrintDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxSimplePrintDirective();
    expect(directive).toBeTruthy();
  });
});
